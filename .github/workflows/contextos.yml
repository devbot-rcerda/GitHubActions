name: Contexto
run-name: ¡Estoy aprendiendo a usar Contextos!
on: push
jobs:
  check-main:
    if: ${{ github.ref != 'refs/heads/main' }}
    runs-on: ubuntu-latest
    steps:
      - name: Run saludos-prueba CI        
        uses: actions/checkout@v3
      - run: echo "Running ${{github.event_name}} only CI using ${{vars.NOMBRE}}. Desplegando en la rama ${{github.ref}}"
